## Database Setup

1. Create a new MySQL database on your system.
2. Import the database using the provided `.sql` file.

Run the following command in your MySQL CLI or use phpMyAdmin to import:
```sql
mysql -u your_mysql_username -p your_database_name < database/your_database_name.sql
